from django.urls import path
from . import views

app_name = "approval"

urlpatterns = [
    # path('first_offs/', views.first_offs, name='first_offs'),
    # path('first_off/create/<int:id>/', views.create_first_off_approval, name='create_first_off_approval'),
    # path('first_off/approve/<int:id>/', views.approve_first_off, name='approve_first_off'),
    # path('first_off/reject/<int:id>/', views.reject_first_off, name='reject_first_off'),
]
